# MIRI Zeiterfassungssystem - Umgebungsvariablen
# Kopiere diese Datei zu .env und passe die Werte an

# Datenbank-Konfiguration
DB_HOST=db
DB_PORT=5432
DB_USER=zes_admin
DB_PASSWORD=Start2025Secure
DB_NAME=zes_db

# Connection Pool Einstellungen
DB_POOL_MAX=20

# Server-Konfiguration
PORT=3001
NODE_ENV=production

# JWT Secret (WICHTIG: Ändere dies in Produktion!)
# Generiere ein sicheres Secret z.B. mit: openssl rand -base64 32
JWT_SECRET=supergeheim

# Timezone
TZ=Europe/Berlin
